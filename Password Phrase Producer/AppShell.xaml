<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Password_Phrase_Producer.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Password_Phrase_Producer"
    xmlns:views="clr-namespace:Password_Phrase_Producer.Views"
    Title="Password Phrase Producer"
    FlyoutBehavior="Flyout"
    FlyoutWidth="260"
    FlyoutBackgroundColor="Transparent"
    Shell.NavBarIsVisible="False"
    FlyoutIcon="{x:Null}">

    <Shell.FlyoutBackdrop>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0D0D12" Offset="0" />
            <GradientStop Color="#14141E" Offset="1" />
        </LinearGradientBrush>
    </Shell.FlyoutBackdrop>

    <Shell.FlyoutBackground>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0.3,1">
            <GradientStop Color="#1A1A24" Offset="0" />
            <GradientStop Color="#12121A" Offset="1" />
        </LinearGradientBrush>
    </Shell.FlyoutBackground>

    <Shell.FlyoutHeader>
        <Grid Padding="16,28,16,16" RowDefinitions="Auto,Auto" RowSpacing="4">
            <Border 
                WidthRequest="36" 
                HeightRequest="36" 
                StrokeThickness="0"
                HorizontalOptions="Start">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="12" />
                </Border.StrokeShape>
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#4A5CFF" Offset="0" />
                        <GradientStop Color="#3B4AD1" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Shadow>
                    <Shadow Brush="#404A5CFF" Radius="10" Offset="0,4" />
                </Border.Shadow>
                <Label 
                    Text="🔐" 
                    FontSize="18" 
                    HorizontalOptions="Center" 
                    VerticalOptions="Center" />
            </Border>
            <VerticalStackLayout Grid.Row="1" Spacing="1" Margin="0,4,0,0">
                <Label 
                    Text="Password Phrase" 
                    FontSize="15" 
                    FontAttributes="Bold" 
                    TextColor="White" />
                <Label 
                    Text="Producer" 
                    FontSize="15" 
                    FontAttributes="Bold" 
                    TextColor="#7B8CFF" />
            </VerticalStackLayout>
        </Grid>
    </Shell.FlyoutHeader>

    <Shell.ItemTemplate>
        <DataTemplate>
            <Grid 
                Padding="14,3" 
                ColumnDefinitions="Auto,*,Auto" 
                ColumnSpacing="10"
                HeightRequest="40">
                <Border 
                    WidthRequest="28" 
                    HeightRequest="28" 
                    BackgroundColor="#252538" 
                    StrokeThickness="0"
                    VerticalOptions="Center">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="8" />
                    </Border.StrokeShape>
                    <Label 
                        Text="{Binding FlyoutIcon}" 
                        FontSize="12" 
                        HorizontalOptions="Center" 
                        VerticalOptions="Center" />
                </Border>
                <Label 
                    Grid.Column="1" 
                    Text="{Binding Title}" 
                    FontSize="13" 
                    TextColor="#E8EBFF" 
                    VerticalOptions="Center" />
                <Label 
                    Grid.Column="2" 
                    Text="›" 
                    FontSize="16" 
                    TextColor="#5A6080" 
                    VerticalOptions="Center" 
                    Margin="0,0,6,0" />
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>

    <Shell.MenuItemTemplate>
        <DataTemplate>
            <Grid 
                Padding="14,3" 
                ColumnDefinitions="Auto,*" 
                ColumnSpacing="10"
                HeightRequest="40">
                <Border 
                    WidthRequest="28" 
                    HeightRequest="28" 
                    BackgroundColor="#252538" 
                    StrokeThickness="0"
                    VerticalOptions="Center">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="8" />
                    </Border.StrokeShape>
                    <Label 
                        Text="{Binding Icon}" 
                        FontSize="12" 
                        HorizontalOptions="Center" 
                        VerticalOptions="Center" />
                </Border>
                <Label 
                    Grid.Column="1" 
                    Text="{Binding Text}" 
                    FontSize="13" 
                    TextColor="#E8EBFF" 
                    VerticalOptions="Center" />
            </Grid>
        </DataTemplate>
    </Shell.MenuItemTemplate>

    <Shell.FlyoutFooter>
        <VerticalStackLayout Padding="16,10,16,20" Spacing="4">
            <BoxView HeightRequest="1" Color="#2A2A3A" Margin="0,0,0,8" />
            <Label 
                Text="Version 1.0" 
                FontSize="10" 
                TextColor="#5A6080" 
                HorizontalOptions="Center" />
        </VerticalStackLayout>
    </Shell.FlyoutFooter>

    <FlyoutItem Title="Startseite"
                Route="home"
                FlyoutIcon="🏠"
                FlyoutDisplayOptions="AsSingleItem">
        <ShellContent
            Title="Startseite"
            ContentTemplate="{DataTemplate local:StartPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Tresor"
                Route="vault"
                FlyoutIcon="🔐"
                FlyoutDisplayOptions="AsSingleItem">
        <ShellContent
            Title="Tresor"
            ContentTemplate="{DataTemplate views:VaultPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Einstellungen"
                Route="settings"
                FlyoutIcon="⚙️"
                FlyoutDisplayOptions="AsSingleItem">
        <ShellContent
            Title="Einstellungen"
            ContentTemplate="{DataTemplate views:SettingsPage}" />
    </FlyoutItem>

</Shell>
