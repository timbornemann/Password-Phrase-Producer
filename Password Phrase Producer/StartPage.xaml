<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Password_Phrase_Producer"
             x:Class="Password_Phrase_Producer.StartPage"
             Padding="0"
             Shell.NavBarIsVisible="False">
    <ContentPage.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#101018" Offset="0" />
            <GradientStop Color="#141426" Offset="0.6" />
            <GradientStop Color="#0F111A" Offset="1" />
        </LinearGradientBrush>
    </ContentPage.Background>

    <ScrollView>
        <VerticalStackLayout x:Name="PageContentLayout"
                             Spacing="18"
                             Padding="16,18,16,18"
                             VerticalOptions="Start">
            
            <!-- Header -->
            <Grid ColumnDefinitions="Auto,*"
                  RowDefinitions="Auto"
                  Padding="0,0,0,6">
                
                <!-- Hamburger Menu -->
                <Border Grid.Column="0"
                        WidthRequest="34"
                        HeightRequest="34"
                        BackgroundColor="#1F2338"
                        StrokeThickness="0"
                        HorizontalOptions="Start"
                        VerticalOptions="Center">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10" />
                    </Border.StrokeShape>
                    <Border.Shadow>
                        <Shadow Brush="#25000000" Radius="6" Offset="0,3" />
                    </Border.Shadow>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnOpenMenuTapped" />
                    </Border.GestureRecognizers>
                    <Image Source="menu.png"
                           WidthRequest="18"
                           HeightRequest="18"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
                </Border>
                
                <VerticalStackLayout Grid.Column="1"
                                      Spacing="2"
                                      Margin="12,0,0,0"
                                      VerticalOptions="Center">
                    <Label Text="Willkommen"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="Was möchtest du tun?"
                           FontSize="12"
                           TextColor="#9EA3C4"/>
                </VerticalStackLayout>
            </Grid>

            <!-- Main Options Container -->
            <VerticalStackLayout Spacing="12">
                
                <!-- Vault Option -->
                <Border StrokeThickness="0"
                        HeightRequest="70"
                        BackgroundColor="Transparent">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="16" />
                    </Border.StrokeShape>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1F2540" Offset="0" />
                            <GradientStop Color="#1B2036" Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Shadow>
                        <Shadow Brush="#25000000" Radius="12" Offset="0,6" />
                    </Border.Shadow>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding NavigateToVaultCommand}" />
                    </Border.GestureRecognizers>
                    
                    <Grid Padding="16" ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" RowSpacing="4">
                        <!-- Text -->
                        <Label Grid.Row="0"
                               Text="Tresor öffnen"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="White"/>
                        <Label Grid.Row="1"
                               Text="Verwalte deine Passwörter."
                               FontSize="12"
                               TextColor="#9EA3C4"
                               LineBreakMode="WordWrap"/>
                        
                        <!-- Arrow -->
                        <Image Grid.Column="1"
                               Grid.RowSpan="2"
                               Source="chevronright.png"
                               WidthRequest="16"
                               HeightRequest="16"
                               VerticalOptions="Center"/>
                    </Grid>
                </Border>

                <!-- Generate Option -->
                <Border StrokeThickness="0"
                        HeightRequest="70"
                        BackgroundColor="Transparent">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="16" />
                    </Border.StrokeShape>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1F2540" Offset="0" />
                            <GradientStop Color="#1B2036" Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Shadow>
                        <Shadow Brush="#25000000" Radius="12" Offset="0,6" />
                    </Border.Shadow>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding NavigateToGenerationCommand}" />
                    </Border.GestureRecognizers>

                    <Grid Padding="16" ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" RowSpacing="4">
                        <!-- Text -->
                        <Label Grid.Row="0"
                               Text="Passwort erstellen"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="White"/>
                        <Label Grid.Row="1"
                               Text="Neue, sichere Passwörter."
                               FontSize="12"
                               TextColor="#9EA3C4"
                               LineBreakMode="WordWrap"/>

                        <!-- Arrow -->
                        <Image Grid.Column="1"
                               Grid.RowSpan="2"
                               Source="chevronright.png"
                               WidthRequest="16"
                               HeightRequest="16"
                               VerticalOptions="Center"/>
                    </Grid>
                </Border>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>