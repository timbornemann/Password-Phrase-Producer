<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Password_Phrase_Producer"
             x:Class="Password_Phrase_Producer.StartPage"
             Padding="0"
             Shell.NavBarIsVisible="False">
    <ContentPage.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#121212" Offset="0" />
            <GradientStop Color="#1C1C24" Offset="0.5" />
            <GradientStop Color="#101018" Offset="1" />
        </LinearGradientBrush>
    </ContentPage.Background>

    <ScrollView>
        <VerticalStackLayout x:Name="PageContentLayout"
                             Spacing="18"
                             Padding="16,18,16,18"
                             VerticalOptions="Start">
            
            <Grid ColumnDefinitions="Auto,*"
                  RowDefinitions="Auto"
                  Padding="0,0,0,6">
                
                <!-- Hamburger Menu (links, konsistent mit Flyout) -->
                <Border Grid.Column="0"
                        WidthRequest="36"
                        HeightRequest="36"
                        BackgroundColor="#1F2336"
                        StrokeThickness="0"
                        HorizontalOptions="Start"
                        VerticalOptions="Center">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="12" />
                    </Border.StrokeShape>
                    <Border.Shadow>
                        <Shadow Brush="#20000000" Radius="6" Offset="0,3" />
                    </Border.Shadow>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnOpenMenuTapped" />
                    </Border.GestureRecognizers>
                    <Label Text="☰"
                           FontSize="16"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"
                           TextColor="#CFD3FF"/>
                </Border>
                
                <VerticalStackLayout Grid.Column="1"
                                      Spacing="2"
                                      Margin="12,0,0,0"
                                      VerticalOptions="Center">
                    <Label Text="Willkommen"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="Was möchtest du tun?"
                           FontSize="12"
                           TextColor="#9EA3C4"/>
                </VerticalStackLayout>
            </Grid>

            <!-- Main Options Container -->
            <VerticalStackLayout Spacing="12">
                
                <!-- Vault Option -->
                <Border StrokeThickness="0"
                        HeightRequest="90"
                        BackgroundColor="Transparent">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="18" />
                    </Border.StrokeShape>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#2D3252" Offset="0" />
                            <GradientStop Color="#1B1E30" Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Shadow>
                        <Shadow Brush="#25000000" Radius="12" Offset="0,6" />
                    </Border.Shadow>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding NavigateToVaultCommand}" />
                    </Border.GestureRecognizers>
                    
                    <Grid Padding="14" ColumnDefinitions="Auto, *" RowDefinitions="*,Auto">
                        <!-- Icon -->
                        <Border Grid.RowSpan="2"
                                WidthRequest="40"
                                HeightRequest="40"
                                VerticalOptions="Center"
                                StrokeThickness="0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="14" />
                            </Border.StrokeShape>
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#4A5CFF" Offset="0" />
                                    <GradientStop Color="#3B4AD1" Offset="1" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Label Text="🔐"
                                   FontSize="20"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"/>
                        </Border>

                        <!-- Text -->
                        <VerticalStackLayout Grid.Column="1" 
                                             Grid.Row="0" 
                                             VerticalOptions="Center"
                                             Padding="12,0,0,0"
                                             Spacing="2">
                            <Label Text="Tresor öffnen"
                                   FontSize="15"
                                   FontAttributes="Bold"
                                   TextColor="White"/>
                            <Label Text="Verwalte deine Passwörter."
                                   FontSize="12"
                                   TextColor="#A0A5C8"
                                   LineBreakMode="WordWrap"/>
                        </VerticalStackLayout>
                        
                        <!-- Arrow -->
                        <Label Grid.Column="1"
                               Grid.Row="1"
                               Text="➜"
                               FontSize="14"
                               TextColor="#5A66FF"
                               HorizontalOptions="End"
                               VerticalOptions="End"/>
                    </Grid>
                </Border>

                <!-- Generate Option -->
                <Border StrokeThickness="0"
                        HeightRequest="90"
                        BackgroundColor="Transparent">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="18" />
                    </Border.StrokeShape>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#362D52" Offset="0" />
                            <GradientStop Color="#221B30" Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Shadow>
                        <Shadow Brush="#25000000" Radius="12" Offset="0,6" />
                    </Border.Shadow>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding NavigateToGenerationCommand}" />
                    </Border.GestureRecognizers>

                    <Grid Padding="14" ColumnDefinitions="Auto, *" RowDefinitions="*,Auto">
                        <!-- Icon -->
                        <Border Grid.RowSpan="2"
                                WidthRequest="40"
                                HeightRequest="40"
                                VerticalOptions="Center"
                                StrokeThickness="0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="14" />
                            </Border.StrokeShape>
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#9D4AFF" Offset="0" />
                                    <GradientStop Color="#7B3BD1" Offset="1" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Label Text="🎲"
                                   FontSize="20"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"/>
                        </Border>

                        <!-- Text -->
                        <VerticalStackLayout Grid.Column="1" 
                                             Grid.Row="0" 
                                             VerticalOptions="Center"
                                             Padding="12,0,0,0"
                                             Spacing="2">
                            <Label Text="Passwort erstellen"
                                   FontSize="15"
                                   FontAttributes="Bold"
                                   TextColor="White"/>
                            <Label Text="Neue, sichere Passwörter."
                                   FontSize="12"
                                   TextColor="#BCA0C8"
                                   LineBreakMode="WordWrap"/>
                        </VerticalStackLayout>

                        <!-- Arrow -->
                        <Label Grid.Column="1"
                               Grid.Row="1"
                               Text="➜"
                               FontSize="14"
                               TextColor="#A65AFF"
                               HorizontalOptions="End"
                               VerticalOptions="End"/>
                    </Grid>
                </Border>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
