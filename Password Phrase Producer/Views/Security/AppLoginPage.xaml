<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Password_Phrase_Producer.Views.Security.AppLoginPage"
             Title="Anmeldung"
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="PrimaryColor">#512BD4</Color>
            <Color x:Key="SecondaryColor">#DFD8F7</Color>
            <Style TargetType="Button">
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HeightRequest" Value="50" />
            </Style>
             <Style TargetType="Entry">
                <Setter Property="TextColor" Value="{AppThemeBinding Light=Black, Dark=White}" />
                <Setter Property="PlaceholderColor" Value="{AppThemeBinding Light=Gray, Dark=LightGray}" />
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#FAFAFA, Dark=#1E1E1E}" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="*,Auto,*" ColumnDefinitions="*">
        <VerticalStackLayout Grid.Row="1" Spacing="20" Padding="30" VerticalOptions="Center">
            
            <Image Source="lock_icon.png" 
                   HeightRequest="100" 
                   WidthRequest="100" 
                   HorizontalOptions="Center"
                   Margin="0,0,0,20"/>

            <Label Text="App gesperrt"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" 
                   TextColor="{AppThemeBinding Light=Black, Dark=White}"/>

            <Label Text="Bitte geben Sie Ihr App-Passwort ein."
                   FontSize="14"
                   HorizontalOptions="Center"
                   TextColor="{AppThemeBinding Light=Gray, Dark=LightGray}" />

            <Entry x:Name="PasswordEntry"
                   Placeholder="Passwort"
                   IsPassword="True"
                   ReturnType="Go"
                   Completed="OnPasswordCompleted" 
                   Margin="0,20,0,0"/>

            <Button Text="Entsperren"
                    Clicked="OnUnlockClicked"
                    BackgroundColor="{StaticResource PrimaryColor}"
                    TextColor="White" />
            
            <Button x:Name="BiometricButton"
                    Text="Biometrie verwenden"
                    Clicked="OnBiometricClicked"
                    BackgroundColor="{StaticResource SecondaryColor}"
                    TextColor="{StaticResource PrimaryColor}"
                    IsVisible="False"
                    ImageSource="fingerprint.png"/>

            <Label x:Name="ErrorLabel"
                   TextColor="Red"
                   HorizontalOptions="Center"
                   IsVisible="False" />

        </VerticalStackLayout>
    </Grid>

</ContentPage>
